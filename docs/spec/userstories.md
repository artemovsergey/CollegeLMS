# CollegeLMS — User Stories

Декомпозиция task.md на пользовательские истории для **5 базовых сервисов**.
Каждая история — атомарная единица работы для одного Agent по протоколу AGENTS.md.

---

## Общие критерии приемки (для всех User Stories)

Применимы ко всем историям ниже, если не указано обратное.

### Авторизация и доступ

| Код | Критерий |
|-----|----------|
| AUTH-1 | `401 Unauthorized` если запрос без JWT токена |
| AUTH-2 | `403 Forbidden` если у пользователя нет прав на операцию |
| AUTH-3 | `404 Not Found` если ресурс не существует (не раскрывать существование) |

### Валидация

| Код | Критерий |
|-----|----------|
| VAL-1 | `400 Bad Request` при невалидных данных (FluentValidation) |
| VAL-2 | Сообщения об ошибках на русском языке |
| VAL-3 | `[Required]`, `[MaxLength]` на всех строковых полях DTO |

### Пагинация (для списков)

| Код | Критерий |
|-----|----------|
| PAG-1 | Параметры `?page={n}&pageSize={m}` (по умолчанию page=1, pageSize=20) |
| PAG-2 | Максимальный pageSize = 100 |
| PAG-3 | Ответ содержит `{ items, totalCount, page, pageSize, totalPages }` |

### UI состояния (для страниц с интерфейсом)

| Код | Критерий |
|-----|----------|
| UI-1 | **Loading**: скелетон/спиннер пока данные загружаются |
| UI-2 | **Empty**: «Нет данных» когда список пуст |
| UI-3 | **Error**: сообщение об ошибке с кнопкой «Повторить» |
| UI-4 | **Success**: тост/уведомление после успешной мутации |

---

## 1. SiteService — Сервис сайта колледжа

### UC-1: Посетитель может просмотреть главную страницу

**Критерии приемки:**
- [ ] Главная страница загружается с информацией о колледже
- [ ] Дизайн соответствует цветам логотипа (DESIGN.md)
- [ ] Страница адаптивна (мобильная/десктопная версия)
- [ ] Меню сайта отображается корректно (иерархия страниц из WordPress)
- [ ] Хедер: логотип, название колледжа, навигация
- [ ] Футер: контакты, ссылки, копирайт
- [ ] UI-1, UI-2, UI-3

**API:** Нет (статическая страница, данные из WordPress импорта)

**UI:**
- Страница `/` с секциями: Hero, Новости, Специальности, Статистика, Партнёры, Обратная связь
- Header с логотипом и навигацией
- Footer с контактами

**Зависимости:** WordPress import data

---

### UC-2: Посетитель может просмотреть список новостей

**Критерии приемки:**
- [ ] `GET /api/news` возвращает список новостей с пагинацией
- [ ] Новости отсортированы по дате (сначала новые)
- [ ] Каждая новость содержит: заголовок, дату, анонс (первые N символов), изображение (preview)
- [ ] `GET /api/news/categories` — список категорий новостей
- [ ] Фильтрация по категории (`?categoryId=`)
- [ ] Поиск по заголовку (`?search=`)
- [ ] PAG-1, PAG-2, PAG-3

**API:**
- `GET /api/news?page={n}&pageSize={m}&categoryId={id}&search={text}` — список новостей
- `GET /api/news/categories` — список категорий

**UI:**
- Страница `/news` со списком новостей, фильтром по категории, поиском
- UI-1, UI-2, UI-3

**Зависимости:** WordPress import

---

### UC-3: Посетитель может просмотреть конкретную новость

**Критерии приемки:**
- [ ] `GET /api/news/{id}` возвращает полную новость (заголовок, дата, полный текст, изображения)
- [ ] AUTH-3 (404 если новость не найдена)

**API:**
- `GET /api/news/{id}` — получить новость по ID

**UI:**
- Страница `/news/[id]` с полным текстом новости
- UI-1, UI-3

**Зависимости:** SiteService

---

### UC-4: Администратор может управлять новостями (CRUD)

**Критерии приемки:**
- [ ] `POST /api/news` — создать новость (только Admin)
- [ ] `PUT /api/news/{id}` — редактировать новость
- [ ] `DELETE /api/news/{id}` — мягкое удаление (IsDeleted = true)
- [ ] AUTH-2 (только Admin)
- [ ] AUTH-3 (404 если новость не найдена)
- [ ] VAL-1, VAL-2, VAL-3 (заголовок обязателен и ≤ 255 символов)

**API:**
- `POST /api/news` — создать новость
- `PUT /api/news/{id}` — обновить новость
- `DELETE /api/news/{id}` — мягко удалить новость

**UI:**
- Страница `/admin/news` с таблицей новостей
- Форма создания/редактирования
- UI-1, UI-4

**Зависимости:** AuthService

---

### UC-5: Посетитель может отправить обратную связь

**Критерии приемки:**
- [ ] `POST /api/feedback` — отправить сообщение (имя, email, сообщение)
- [ ] Без авторизации (публичная форма)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `POST /api/feedback` — создание сообщения обратной связи

**UI:**
- Форма обратной связи на главной странице (имя, email, сообщение)
- UI-1, UI-3, UI-4

**Зависимости:** нет

---

### UC-6: Администратор может импортировать данные с WordPress

**Критерии приемки:**
- [ ] `POST /api/import/wordpress` — запустить импорт (только Admin)
- [ ] Импорт из JSON (структура: страницы, категории, меню)
- [ ] Импорт полного контента через WordPress REST API
- [ ] Импорт идемпотентен (повторный запуск не создаёт дубликатов — проверка по WP ID)
- [ ] Прогресс импорта: `{ total, processed, errors }`
- [ ] AUTH-2 (только Admin)

**API:**
- `POST /api/import/wordpress` — запустить импорт
- `GET /api/import/status` — проверить статус текущего импорта

**UI:**
- Страница `/admin/import` с кнопкой запуска и индикатором прогресса
- UI-1, UI-3, UI-4

**Зависимости:** WordPress REST API (live), FileService

---

### UC-7: Посетитель может просмотреть статические страницы сайта

**Критерии приемки:**
- [ ] Статические страницы рендерятся в Next.js (статическая генерация, без API)
- [ ] Страницы содержат: заголовок, содержимое (HTML), изображения
- [ ] Без авторизации (публичный доступ)
- [ ] Хлебные крошки отражают иерархию страниц
- [ ] Навигация в Header отображает все разделы сайта

**API:** Нет (статические страницы, данные из WordPress импорта)

**UI:**
- Страницы: `/about/[[...slug]]`, `/college/[[...slug]]`, `/education/[[...slug]]`, `/admissions/[[...slug]]`, `/graduates/[[...slug]]`, `/student-life/[[...slug]]`, `/partners`
- Хлебные крошки для навигации по иерархии
- UI-1, UI-2, UI-3

**Зависимости:** WordPress import data

---

### UC-8: Посетитель может выполнить поиск по сайту

**Критерии приемки:**
- [ ] `GET /api/search?q={query}` — полнотекстовый поиск по новостям и статическим страницам
- [ ] Результаты: заголовок, тип контента (news/page), url, сниппет (первые 200 символов с подсветкой)
- [ ] Сортировка по релевантности
- [ ] Пустой запрос возвращает пустой результат (не ошибку)
- [ ] PAG-1, PAG-2, PAG-3
- [ ] Rate limiting: не более 30 запросов в минуту с одного IP

**API:**
- `GET /api/search?q={query}&page={n}&pageSize={m}` — поиск

**UI:**
- Поле поиска в Header
- Страница `/search?q={query}` с результатами
- Автоподсказки при вводе (debounce 300ms)
- Пустое состояние: «Ничего не найдено» при отсутствии результатов
- UI-1, UI-2, UI-3

**Зависимости:** News, Pages (WordPress import data)

---

## 2. AuthService — Сервис аутентификации и авторизации

### UC-9: Пользователь может войти по логину и паролю

**Критерии приемки:**
- [ ] `POST /api/auth/login` — принимает `{ login, password }`
- [ ] Возвращает JWT токен (access token, срок действия 24ч)
- [ ] Неверный логин или пароль → 401 (AUTH-1)
- [ ] Пароль хешируется BCrypt
- [ ] Rate limiting: не более 5 попыток в минуту с одного IP
- [ ] VAL-1, VAL-2, VAL-3 (логин обязателен, пароль обязателен)

**API:**
- `POST /api/auth/login` — аутентификация, возврат JWT + профиль `{ token, user: { id, login, fullName, role } }`

**UI:**
- Страница `/login` с формой входа (логин, пароль)
- Выпадающий список «Быстрый вход» для 4 ролей (Admin/Teacher/Student/Dispatcher)
- Dispatcher → `/schedule` после входа, остальные → `/my/dashboard`
- UI-1, UI-3, UI-4

**Зависимости:** AuthService, Redis (rate limiting)

---

### UC-10: Пользователь может выйти из системы

**Критерии приемки:**
- [ ] `POST /api/auth/logout` — инвалидирует токен (добавляет в Redis blacklist)
- [ ] Запрос с невалидным/истёкшим токеном → 401 (AUTH-1)

**API:**
- `POST /api/auth/logout` — выход из системы

**UI:**
- Кнопка «Выйти» в Header/меню пользователя
- UI-4 (тост «Вы вышли из системы»)

**Зависимости:** Redis

---

### UC-11: Пользователь может получить свой профиль

**Критерии приемки:**
- [ ] `GET /api/auth/profile` — возвращает данные текущего пользователя
- [ ] AUTH-1 (требуется JWT токен)
- [ ] AUTH-3 (404 только если пользователь удалён/деактивирован)

**API:**
- `GET /api/auth/profile` — профиль текущего пользователя

**UI:**
- Отображение профиля в Header (имя, роль)
- UI-1, UI-3

**Зависимости:** AuthService

---

### UC-12: Администратор может управлять пользователями (CRUD)

**Критерии приемки:**
- [ ] `GET /api/users` — список пользователей с фильтрацией по роли
- [ ] `POST /api/users` — создать пользователя (логин, пароль, ФИО, роль)
- [ ] `PUT /api/users/{id}` — редактировать пользователя
- [ ] `DELETE /api/users/{id}` — деактивировать пользователя (IsActive = false, не удалять физически)
- [ ] PAG-1, PAG-2, PAG-3
- [ ] AUTH-2 (только Admin)
- [ ] AUTH-3 (404 если пользователь не найден)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/users?role={role}&page={n}&pageSize={m}` — список
- `POST /api/users` — создать
- `PUT /api/users/{id}` — обновить
- `DELETE /api/users/{id}` — деактивировать

**UI:**
- Страница `/admin/users` с таблицей пользователей и формой создания/редактирования
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** AuthService

---

### UC-13: Администратор может сменить роль пользователя

**Критерии приемки:**
- [ ] `PATCH /api/users/{id}/role` — изменить роль
- [ ] AUTH-2 (только Admin)
- [ ] AUTH-3 (404 если пользователь не найден)
- [ ] VAL-1, VAL-2 (роль должна быть из списка: Admin, Teacher, Student, Dispatcher)

**API:**
- `PATCH /api/users/{id}/role` — смена роли

**UI:**
- Компонент выбора роли на странице редактирования пользователя
- UI-4 (тост «Роль изменена»)

**Зависимости:** AuthService

---

### UC-14: Пользователь может сменить пароль

**Критерии приемки:**
- [ ] `POST /api/auth/change-password` — смена пароля (старый пароль, новый пароль)
- [ ] Неверный старый пароль → 400 Bad Request
- [ ] Новый пароль: минимум 8 символов, VAL-1, VAL-2
- [ ] AUTH-1 (требуется JWT)

**API:**
- `POST /api/auth/change-password` — смена пароля

**UI:**
- Форма смены пароля в настройках профиля (страница `/my/settings`)
- UI-1, UI-3, UI-4

**Зависимости:** AuthService

---

### UC-15: Администратор может заблокировать/разблокировать пользователя

**Критерии приемки:**
- [ ] `PATCH /api/users/{id}/toggle-active` — переключить IsActive
- [ ] Заблокированный пользователь не может войти в систему (401)
- [ ] AUTH-2 (только Admin)
- [ ] AUTH-3 (404 если пользователь не найден)

**API:**
- `PATCH /api/users/{id}/toggle-active` — блокировка/разблокировка

**UI:**
- Переключатель «Активен» на странице редактирования пользователя
- UI-4 (тост «Пользователь заблокирован/разблокирован»)

**Зависимости:** AuthService

---

## 3. ScheduleService — Сервис расписания занятий

### UC-16: Студент/Преподаватель может просмотреть расписание по группе

**Критерии приемки:**
- [ ] `GET /api/schedule?groupId={id}` — расписание для группы
- [ ] Отсортировано по дню недели и времени начала
- [ ] Каждая запись: предмет, преподаватель, аудитория, время начала/конца, день недели, тип занятия (лекция/практика)
- [ ] PAG-1, PAG-2, PAG-3
- [ ] AUTH-1

**API:**
- `GET /api/schedule?groupId={id}&page={n}&pageSize={m}` — расписание группы

**UI:**
- Страница `/schedule` с фильтрами (группа, преподаватель, день недели)
- UI-1, UI-2, UI-3

**Зависимости:** GroupService (LearningService)

---

### UC-17: Пользователь может просмотреть расписание по преподавателю

**Критерии приемки:**
- [ ] `GET /api/schedule?teacherId={id}` — расписание преподавателя
- [ ] AUTH-3 (404 если преподаватель не найден)
- [ ] AUTH-1

**API:**
- `GET /api/schedule?teacherId={id}&page={n}&pageSize={m}` — расписание преподавателя

**UI:**
- Фильтр «Преподаватель» на странице `/schedule`
- UI-1, UI-2, UI-3

**Зависимости:** TeacherService (LearningService)

---

### UC-18: Пользователь может просмотреть расписание по аудитории

**Критерии приемки:**
- [ ] `GET /api/schedule?room={name}` — расписание для аудитории
- [ ] Пустой результат если записей нет (не 404)
- [ ] AUTH-1

**API:**
- `GET /api/schedule?room={name}&page={n}&pageSize={m}` — расписание аудитории

**UI:**
- Фильтр «Аудитория» на странице `/schedule`
- UI-1, UI-2, UI-3

**Зависимости:** ScheduleService

---

### UC-19: Диспетчер может управлять записями расписания (CRUD)

**Критерии приемки:**
- [ ] `POST /api/schedule` — создать запись (только Dispatcher/Admin)
- [ ] `PUT /api/schedule/{id}` — редактировать запись
- [ ] `DELETE /api/schedule/{id}` — удалить запись
- [ ] AUTH-2 (только Dispatcher/Admin)
- [ ] AUTH-3 (404 если запись не найдена)
- [ ] Валидация пересечений: одна группа не может быть в двух местах одновременно, преподаватель не может вести две пары одновременно, аудитория не может быть занята
- [ ] Валидация StartTime < EndTime (400 Bad Request)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `POST /api/schedule` — создать
- `PUT /api/schedule/{id}` — обновить
- `DELETE /api/schedule/{id}` — удалить

**UI:**
- `ScheduleEntryDialog` — модальное окно создания/редактирования
- `AlertDialog` подтверждения удаления
- Все сообщения об ошибках — в тосте (Sonner)
- UI-1, UI-3, UI-4

**Зависимости:** AuthService (роль Dispatcher)

---

### UC-20: Пользователь может смотреть расписание на день/неделю/месяц/семестр/год

**Критерии приемки:**
- [ ] `GET /api/schedule?dayOfWeek={n}` — на день (по дню недели)
- [ ] `GET /api/schedule?period=week` — на неделю
- [ ] `GET /api/schedule?period=month` — на месяц
- [ ] `GET /api/schedule?period=semester&semesterId={id}` — на семестр
- [ ] `GET /api/schedule?period=year` — на год
- [ ] Фильтрация по groupId/teacherId/room комбинируется с period и semesterId
- [ ] AUTH-1

**API:**
- `GET /api/schedule?groupId={id}&dayOfWeek={n}&period={week|month|semester|year}&semesterId={id}` — расписание за период

**UI:**
- Переключатель периодов (день/неделя/месяц/семестр/год) на странице `/schedule`
- UI-1, UI-2, UI-3

**Зависимости:** ScheduleService, SemesterService

---

### UC-21: Пользователь может экспортировать расписание (PDF/Excel)

**Критерии приемки:**
- [ ] `GET /api/schedule/export?groupId={id}&format=pdf` — скачать PDF
- [ ] `GET /api/schedule/export?groupId={id}&format=xlsx` — скачать Excel
- [ ] Файл генерируется на сервере и возвращается как download (Content-Disposition: attachment)
- [ ] PDF через QuestPDF, XLSX через ClosedXML
- [ ] AUTH-1
- [ ] AUTH-3 (404 если группа/преподаватель не найдены)

**API:**
- `GET /api/schedule/export?groupId={id}&teacherId={id}&room={name}&period={week|month}&format={pdf|xlsx}` — экспорт

**UI:**
- Кнопки экспорта PDF/Excel в панели фильтров на странице `/schedule`
- UI-1, UI-3

**Зависимости:** ScheduleService

---

### UC-22: Диспетчер может импортировать расписание из XLSX

**Критерии приемки:**
- [ ] `POST /api/schedule/import` — загрузить файл расписания (multipart/form-data)
- [ ] Поддерживаемый формат: xlsx
- [ ] AUTH-2 (только Dispatcher/Admin)
- [ ] Валидация: проверка на пересечения, пропущенные поля
- [ ] Импорт идемпотентен (заменяет существующие записи на дату, не дублирует)
- [ ] Ответ: `{ imported: N, skipped: N, errors: [{ row, message }] }`

**API:**
- `POST /api/schedule/import` — импорт расписания

**UI:**
- `ScheduleImportDialog` — диалог загрузки XLSX с результатом
- UI-1, UI-3, UI-4

**Зависимости:** FileService, ScheduleService

---

### UC-23: Пользователь может просмотреть календарную сетку расписания

**Критерии приемки:**
- [ ] `GET /api/schedule?view=calendar` — календарный вид (сетка/таблица)
- [ ] Отображение занятий в сетке по дням недели и времени
- [ ] Поддержка переключения между неделями (предыдущая/следующая)
- [ ] Комбинируется с groupId/teacherId/room
- [ ] AUTH-1

**API:**
- `GET /api/schedule?groupId={id}&view=calendar` — календарная сетка

**UI:**
- Переключатель вида «Список» / «Календарь» на странице `/schedule`
- Календарная сетка с ячейками занятий
- UI-1, UI-2, UI-3

**Зависимости:** ScheduleService

---

## 4. LearningService — Сервис учебного процесса

### UC-24: Администратор может управлять группами (CRUD)

**Критерии приемки:**
- [ ] `GET /api/groups` — список групп с пагинацией
- [ ] `POST /api/groups` — создать группу (название, курс 1-4)
- [ ] `PUT /api/groups/{id}` — редактировать
- [ ] `DELETE /api/groups/{id}` — мягкое удаление
- [ ] PAG-1, PAG-2, PAG-3
- [ ] AUTH-2 (только Admin)
- [ ] AUTH-3 (404 если группа не найдена)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/groups?page={n}&pageSize={m}` — список
- `POST /api/groups` — создать
- `PUT /api/groups/{id}` — обновить
- `DELETE /api/groups/{id}` — удалить

**UI:**
- Страница `/groups` с таблицей групп и формой создания/редактирования
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** LearningService

---

### UC-25: Администратор может управлять студентами (CRUD)

**Критерии приемки:**
- [ ] `GET /api/students` — список студентов (с фильтром по группе)
- [ ] `POST /api/students` — добавить студента (создаётся User + Student)
- [ ] `PUT /api/students/{id}` — редактировать
- [ ] Студент: ФИО, группа, номер зачётки
- [ ] PAG-1, PAG-2, PAG-3
- [ ] AUTH-2 (только Admin)
- [ ] AUTH-3 (404 если студент не найден)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/students?groupId={id}&page={n}&pageSize={m}` — список
- `POST /api/students` — создать
- `PUT /api/students/{id}` — обновить

**UI:**
- Страница `/students` с таблицей студентов и формой
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** AuthService, GroupService

---

### UC-26: Администратор может управлять преподавателями (CRUD)

**Критерии приемки:**
- [ ] `GET /api/teachers` — список преподавателей
- [ ] `POST /api/teachers` — добавить преподавателя (создаётся User + Teacher)
- [ ] `PUT /api/teachers/{id}` — редактировать
- [ ] Преподаватель: ФИО, кафедра, должность
- [ ] PAG-1, PAG-2, PAG-3
- [ ] AUTH-2 (только Admin)
- [ ] AUTH-3 (404 если преподаватель не найден)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/teachers?page={n}&pageSize={m}` — список
- `POST /api/teachers` — создать
- `PUT /api/teachers/{id}` — обновить

**UI:**
- Страница `/teachers` с таблицей преподавателей и формой
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** AuthService

---

### UC-27: Преподаватель может создать учебный курс

**Критерии приемки:**
- [ ] `POST /api/courses` — создать курс (автор = текущий пользователь)
- [ ] `GET /api/courses` — список курсов (фильтр по преподавателю/группе/статусу)
- [ ] `GET /api/courses/{id}` — детали курса
- [ ] `PUT /api/courses/{id}` — редактировать
- [ ] `DELETE /api/courses/{id}` — удалить курс
- [ ] Курс: название, описание, преподаватель (автор), группа, статус (Draft/Active/Archived)
- [ ] AUTH-2 (только автор курса или Admin может редактировать/удалять)
- [ ] AUTH-3 (404 если курс не найден)
- [ ] PAG-1, PAG-2, PAG-3
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/courses?teacherId={id}&groupId={id}&status={status}&page={n}&pageSize={m}` — список
- `POST /api/courses` — создать
- `GET /api/courses/{id}` — детали
- `PUT /api/courses/{id}` — обновить
- `DELETE /api/courses/{id}` — удалить

**UI:**
- Страница `/courses` (список)
- Страница `/courses/new` (создание)
- Страница `/courses/[id]/edit` (редактирование)
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** GroupService, TeacherService, AuthService

---

### UC-28: Преподаватель может управлять лекциями курса

**Критерии приемки:**
- [ ] `POST /api/courses/{courseId}/lectures` — добавить лекцию
- [ ] `GET /api/courses/{courseId}/lectures` — список лекций (сортировка по порядковому номеру)
- [ ] `GET /api/courses/{courseId}/lectures/{id}` — просмотр лекции
- [ ] `PUT /api/courses/{courseId}/lectures/{id}` — редактировать
- [ ] `DELETE /api/courses/{courseId}/lectures/{id}` — удалить
- [ ] Лекция: тема, содержание, порядковый номер
- [ ] AUTH-2 (только автор курса или Admin)
- [ ] AUTH-3 (404 если курс/лекция не найдена)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/courses/{courseId}/lectures` — список лекций
- `POST /api/courses/{courseId}/lectures` — создать
- `GET /api/courses/{courseId}/lectures/{id}` — детали
- `PUT /api/courses/{courseId}/lectures/{id}` — обновить
- `DELETE /api/courses/{courseId}/lectures/{id}` — удалить

**UI:**
- Страница `/courses/[id]/lectures` (список)
- Страница `/courses/[id]/lectures/new` (создание)
- Страница `/courses/[id]/lectures/[lectureId]` (просмотр)
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** CourseService

---

### UC-29: Преподаватель может управлять заданиями курса

**Критерии приемки:**
- [ ] `POST /api/courses/{courseId}/assignments` — создать задание
- [ ] `GET /api/courses/{courseId}/assignments` — список заданий курса
- [ ] `GET /api/courses/{courseId}/assignments/{id}` — просмотр задания
- [ ] `PUT /api/courses/{courseId}/assignments/{id}` — редактировать
- [ ] `DELETE /api/courses/{courseId}/assignments/{id}` — удалить
- [ ] Задание: название, описание, срок сдачи (dueDate), макс. балл (1-100), порядковый номер
- [ ] AUTH-2 (только автор курса или Admin)
- [ ] AUTH-3 (404 если курс/задание не найдено)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/courses/{courseId}/assignments` — список
- `POST /api/courses/{courseId}/assignments` — создать
- `GET /api/courses/{courseId}/assignments/{id}` — детали
- `PUT /api/courses/{courseId}/assignments/{id}` — обновить
- `DELETE /api/courses/{courseId}/assignments/{id}` — удалить

**UI:**
- Страница `/courses/[id]/assignments` (список)
- Страница `/courses/[id]/assignments/new` (создание)
- Страница `/courses/[id]/assignments/[assignmentId]` (детали)
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** CourseService

---

### UC-30: Студент может просмотреть свои курсы

**Критерии приемки:**
- [ ] `GET /api/my/courses` — список курсов текущего студента (по его группе)
- [ ] `GET /api/my/courses/{id}` — детали курса с лекциями и заданиями
- [ ] AUTH-1
- [ ] AUTH-3 (404 если курс не найден или студент не в группе курса)

**API:**
- `GET /api/my/courses` — мои курсы
- `GET /api/my/courses/{id}` — детали курса

**UI:**
- Страница `/my/courses` (список)
- Страница `/my/courses/[id]` (детали курса)
- UI-1, UI-2, UI-3

**Зависимости:** CourseService, GroupService

---

### UC-31: Студент может просмотреть лекции и материалы курса

**Критерии приемки:**
- [ ] `GET /api/courses/{courseId}/lectures` — список лекций курса
- [ ] `GET /api/courses/{courseId}/lectures/{id}` — просмотр лекции
- [ ] `GET /api/courses/{courseId}/materials` — список файлов курса
- [ ] `GET /api/materials/{id}/download` — скачать файл
- [ ] AUTH-2 (только студенты группы курса имеют доступ)
- [ ] AUTH-3 (404 если курс/лекция/материал не найден)

**API:**
- `GET /api/courses/{courseId}/lectures` — лекции
- `GET /api/courses/{courseId}/lectures/{id}` — лекция
- `GET /api/courses/{courseId}/materials` — материалы
- `GET /api/materials/{id}/download` — скачать файл

**UI:**
- Страница `/courses/[id]` (вкладки «Лекции» и «Материалы»)
- UI-1, UI-2, UI-3

**Зависимости:** FileService, CourseService

---

### UC-32: Студент может отправить выполненное задание

**Критерии приемки:**
- [ ] `POST /api/assignments/{id}/submit` — загрузить выполненное задание (multipart + comment)
- [ ] `GET /api/assignments/{id}/submissions` (студент) — свои отправки по заданию
- [ ] `GET /api/my/submissions` — все свои отправки
- [ ] Студент может прикрепить файл и оставить комментарий
- [ ] Повторная отправка: создаёт новую версию (не заменяет предыдущую)
- [ ] AUTH-2 (только студенты группы курса)
- [ ] AUTH-3 (404 если задание не найдено)
- [ ] VAL-1, VAL-2

**API:**
- `POST /api/assignments/{id}/submit` — отправить задание
- `GET /api/my/submissions` — мои отправки

**UI:**
- Страница `/courses/[id]/assignments/[assignmentId]` (форма отправки)
- Страница `/my/submissions` (список всех отправок)
- UI-1, UI-3, UI-4

**Зависимости:** FileService

---

### UC-33: Преподаватель может оценивать отправки студентов

**Критерии приемки:**
- [ ] `GET /api/assignments/{id}/submissions` — список отправок по заданию (преподаватель видит всех студентов группы)
- [ ] `PATCH /api/submissions/{id}/grade` — выставить оценку (от 0 до макс. балла задания)
- [ ] AUTH-2 (только преподаватель-автор курса)
- [ ] AUTH-3 (404 если отправка не найдена)
- [ ] VAL-1, VAL-2

**API:**
- `GET /api/assignments/{id}/submissions` — список отправок
- `PATCH /api/submissions/{id}/grade` — оценить

**UI:**
- Страница `/courses/[id]/assignments/[assignmentId]/submissions` (список студентов с отправками)
- UI-1, UI-3, UI-4

**Зависимости:** CourseService

---

### UC-34: Студент может просмотреть личный кабинет

**Критерии приемки:**
- [ ] `GET /api/my/dashboard` — сводка: курсы, дедлайны, недавние оценки
- [ ] `GET /api/my/submissions` — список всех отправок с оценками
- [ ] AUTH-1

**API:**
- `GET /api/my/dashboard` — дашборд студента
- `GET /api/my/submissions` — мои отправки

**UI:**
- Страница `/my/dashboard` с карточками курсов, дедлайнами, последними оценками
- UI-1, UI-2, UI-3

**Зависимости:** CourseService, SubmissionService

---

### UC-35: Преподаватель может просмотреть личный кабинет

**Критерии приемки:**
- [ ] `GET /api/teacher/dashboard` — сводка: курсы, студенты, недавние отправки (ожидающие проверки, ≤ 10)
- [ ] Сводка содержит текущую нагрузку: количество курсов, групп, студентов
- [ ] Сводка содержит базовое портфолио: повышения квалификации, грамоты, категории, сертификаты
- [ ] AUTH-1 (только Teacher/Admin)

**API:**
- `GET /api/teacher/dashboard` — дашборд преподавателя

**UI:**
- Страница `/teacher/dashboard` со списком курсов, портфолио и нагрузкой
- UI-1, UI-2, UI-3

**Зависимости:** CourseService, SubmissionService

---

### UC-36: Преподаватель может загружать файлы к курсу

**Критерии приемки:**
- [ ] `POST /api/courses/{courseId}/materials` — загрузить файл (multipart/form-data)
- [ ] `GET /api/courses/{courseId}/materials` — список файлов курса
- [ ] `GET /api/materials/{id}/download` — скачать файл
- [ ] `DELETE /api/materials/{id}` — удалить файл
- [ ] Файл: имя, размер, MIME-тип, дата загрузки
- [ ] AUTH-2 (только автор курса или Admin)
- [ ] AUTH-3 (404 если материал не найден)
- [ ] VAL-1 (проверка MIME-типа: PDF, video, office docs)

**API:**
- `POST /api/courses/{courseId}/materials` — загрузить
- `GET /api/courses/{courseId}/materials` — список
- `GET /api/materials/{id}/download` — скачать
- `DELETE /api/materials/{id}` — удалить

**UI:**
- Вкладка «Материалы» на странице курса
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** FileService

---

### UC-37: Администратор может управлять учебными семестрами

**Критерии приемки:**
- [ ] `POST /api/semesters` — создать семестр (название, дата начала, дата конца, тип: осенний/весенний)
- [ ] `GET /api/semesters` — список семестров (сортировка по дате начала)
- [ ] `PUT /api/semesters/{id}` — редактировать
- [ ] `DELETE /api/semesters/{id}` — удалить
- [ ] Семестр: название, дата начала, дата конца, тип (Autumn/Spring), учебный год
- [ ] AUTH-2 (только Admin)
- [ ] AUTH-3 (404 если семестр не найден)
- [ ] VAL-1, VAL-2, VAL-3
- [ ] Дата начала < Дата конца

**API:**
- `GET /api/semesters?page={n}&pageSize={m}` — список
- `POST /api/semesters` — создать
- `PUT /api/semesters/{id}` — обновить
- `DELETE /api/semesters/{id}` — удалить

**UI:**
- Страница `/admin/semesters` с таблицей и формой
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** LearningService

---

### UC-38: Администратор может массово импортировать студентов из Excel

**Критерии приемки:**
- [ ] `POST /api/students/import` — загрузить XLSX со студентами (multipart/form-data)
- [ ] Поля: ФИО, группа (название), номер зачётки, логин (генерируется автоматически если не указан)
- [ ] Для каждого студента создаётся User + Student
- [ ] Валидация: группа должна существовать, ФИО обязательно
- [ ] Импорт идемпотентен (проверка по номеру зачётки)
- [ ] Ответ: `{ imported: N, skipped: N, errors: [{ row, message }] }`
- [ ] AUTH-2 (только Admin)

**API:**
- `POST /api/students/import` — импорт студентов

**UI:**
- Страница `/admin/students/import` с формой загрузки и результатом
- UI-1, UI-3, UI-4

**Зависимости:** GroupService, AuthService

---

## 5. TestingService — Сервис тестирования

### UC-39: Преподаватель может создать тест

**Критерии приемки:**
- [ ] `POST /api/tests` — создать тест (в рамках курса)
- [ ] `GET /api/tests` — список тестов (с фильтром по курсу)
- [ ] `GET /api/tests/{id}` — детали теста
- [ ] `PUT /api/tests/{id}` — редактировать
- [ ] `DELETE /api/tests/{id}` — удалить тест
- [ ] Тест: название, описание, время на прохождение (минуты), количество попыток, тип (control/self-study), проходной балл
- [ ] AUTH-2 (только преподаватель-автор курса или Admin)
- [ ] AUTH-3 (404 если курс/тест не найден)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/tests?courseId={id}&page={n}&pageSize={m}` — список
- `POST /api/tests` — создать
- `GET /api/tests/{id}` — детали
- `PUT /api/tests/{id}` — обновить
- `DELETE /api/tests/{id}` — удалить

**UI:**
- Раздел «Тесты» на странице курса
- Форма создания/редактирования теста
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** CourseService

---

### UC-40: Преподаватель может добавить и настроить вопросы теста

**Критерии приемки:**
- [ ] `POST /api/tests/{testId}/questions` — добавить вопрос
- [ ] `GET /api/tests/{testId}/questions` — список вопросов
- [ ] `PUT /api/tests/{testId}/questions/{id}` — редактировать
- [ ] `DELETE /api/tests/{testId}/questions/{id}` — удалить вопрос
- [ ] Типы вопросов: SingleChoice (одиночный выбор), MultipleChoice (множественный выбор), OpenAnswer (открытый ответ)
- [ ] Вопрос: текст, тип, варианты ответов (для выбора), правильный ответ/ответы, баллы
- [ ] AUTH-2 (только автор курса или Admin)
- [ ] AUTH-3 (404 если тест/вопрос не найден)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/tests/{testId}/questions` — список вопросов
- `POST /api/tests/{testId}/questions` — создать
- `PUT /api/tests/{testId}/questions/{id}` — обновить
- `DELETE /api/tests/{testId}/questions/{id}` — удалить

**UI:**
- Страница `/courses/[id]/tests/[testId]/questions` с конструктором вопросов
- Добавление/редактирование вопроса в модальном окне
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** TestingService

---

### UC-41: Преподаватель может назначить тест группе

**Критерии приемки:**
- [ ] `POST /api/tests/{testId}/assign` — назначить тест группе
- [ ] Параметры: группаId, дата открытия, дата закрытия, доступные попытки
- [ ] `GET /api/tests/{testId}/assignments` — список назначений теста
- [ ] `DELETE /api/tests/{testId}/assign/{assignmentId}` — отменить назначение
- [ ] AUTH-2 (только автор курса или Admin)
- [ ] AUTH-3 (404 если тест/группа не найдены)
- [ ] VAL-1, VAL-2 (дата закрытия > даты открытия)

**API:**
- `POST /api/tests/{testId}/assign` — назначить
- `GET /api/tests/{testId}/assignments` — список назначений
- `DELETE /api/tests/{testId}/assign/{assignmentId}` — отменить

**UI:**
- Раздел «Назначения» на странице теста
- Выбор группы, дат, количества попыток
- UI-1, UI-3, UI-4

**Зависимости:** GroupService

---

### UC-42: Студент может пройти тест с ограничением по времени

**Критерии приемки:**
- [ ] `GET /api/tests/{testId}/start` — начать тест (фиксируется время начала, блокируется повторный старт если превышены попытки)
- [ ] `GET /api/tests/{testId}/attempt/{attemptId}` — получить вопросы теста (без правильных ответов)
- [ ] `POST /api/tests/{testId}/attempt/{attemptId}/submit` — отправить ответы
- [ ] Автоматическая проверка закрытых вопросов (SingleChoice, MultipleChoice)
- [ ] Ограничение по времени: при истечении — 408 Request Timeout, попытка фиксируется как сданная
- [ ] Ограничение по количеству попыток: при превышении — 409 Conflict
- [ ] AUTH-2 (только студенты группы, которой назначен тест)
- [ ] AUTH-3 (404 если тест не найден)
- [ ] UI-2 (если тест не содержит вопросов — нельзя начать)

**API:**
- `GET /api/tests/{testId}/start` — начать
- `GET /api/tests/{testId}/attempt/{attemptId}` — получить вопросы
- `POST /api/tests/{testId}/attempt/{attemptId}/submit` — отправить ответы

**UI:**
- Страница `/courses/[id]/tests/[testId]` с кнопкой «Начать тест»
- Интерфейс прохождения: вопрос за вопросом (или все сразу)
- Таймер обратного отсчёта
- UI-1, UI-3, UI-4

**Зависимости:** TestingService

---

### UC-43: Студент может просмотреть результаты теста

**Критерии приемки:**
- [ ] `GET /api/tests/{testId}/results` — результат текущего студента по тесту (баллы, правильные ответы, ошибки)
- [ ] `GET /api/my/test-results` — все результаты студента по всем тестам
- [ ] После отправки теста — автоматический показ результата
- [ ] AUTH-1

**API:**
- `GET /api/tests/{testId}/results` — результат по тесту
- `GET /api/my/test-results` — все результаты

**UI:**
- Страница `/my/test-results` (список всех результатов)
- Страница с результатом после завершения теста: баллы, правильные/неправильные ответы
- UI-1, UI-2, UI-3

**Зависимости:** TestingService

---

### UC-44: Преподаватель может просмотреть статистику по тесту

**Критерии приемки:**
- [ ] `GET /api/tests/{testId}/stats` — статистика: средний балл, медиана, распределение оценок, список не сдавших (балл < проходного)
- [ ] Экспорт статистики в PDF/Excel
- [ ] AUTH-2 (только автор курса или Admin)
- [ ] AUTH-3 (404 если тест не найден)

**API:**
- `GET /api/tests/{testId}/stats` — статистика
- `GET /api/tests/{testId}/stats/export?format={pdf|xlsx}` — экспорт

**UI:**
- Раздел «Статистика» на странице теста с графиками и таблицами
- Кнопка экспорта
- UI-1, UI-2, UI-3

**Зависимости:** TestingService

---

### UC-45: Преподаватель может настроить автопроверку и проходной балл

**Критерии приемки:**
- [ ] `PUT /api/tests/{testId}/settings` — настроить параметры проверки
- [ ] Параметры: проходной балл (0-100), автопроверка (вкл/выкл), показывать ли правильные ответы после завершения
- [ ] Если автопроверка выключена — ответы на открытые вопросы проверяет преподаватель вручную
- [ ] Перемешивание вопросов (вкл/выкл)
- [ ] Перемешивание вариантов ответов (вкл/выкл)
- [ ] AUTH-2 (только автор курса или Admin)
- [ ] AUTH-3 (404 если тест не найден)
- [ ] VAL-1, VAL-2 (проходной балл 0-100)

**API:**
- `PUT /api/tests/{testId}/settings` — настройки теста

**UI:**
- Раздел «Настройки» на странице теста
- Переключатели автопроверки, перемешивания, поле проходного балла
- UI-1, UI-3, UI-4

**Зависимости:** TestingService

---

### UC-46: Заведующий отделением может управлять специальностями

**Критерии приемки:**
- [ ] `GET /api/specialties` — список специальностей с пагинацией
- [ ] `POST /api/specialties` — создать специальность (код, название, описание, отделение)
- [ ] `PUT /api/specialties/{id}` — редактировать специальность
- [ ] `DELETE /api/specialties/{id}` — мягкое удаление
- [ ] Специальность: код (например, 09.02.07), название, описание, отделение
- [ ] PAG-1, PAG-2, PAG-3
- [ ] AUTH-2 (только Admin / Заведующий отделением)
- [ ] AUTH-3 (404 если специальность не найдена)
- [ ] VAL-1, VAL-2, VAL-3

**API:**
- `GET /api/specialties?page={n}&pageSize={m}&search={text}` — список
- `POST /api/specialties` — создать
- `PUT /api/specialties/{id}` — обновить
- `DELETE /api/specialties/{id}` — удалить

**UI:**
- Страница `/admin/specialties` с таблицей специальностей и формой
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** LearningService

---

### UC-47: Преподаватель может назначить курс группе

**Критерии приемки:**
- [ ] `POST /api/courses/{courseId}/groups` — привязать курс к группе (одной или нескольким)
- [ ] `GET /api/courses/{courseId}/groups` — список групп, привязанных к курсу
- [ ] `DELETE /api/courses/{courseId}/groups/{groupId}` — отвязать группу от курса
- [ ] Студенты привязанной группы получают доступ к курсу (лекции, задания, тесты)
- [ ] AUTH-2 (только автор курса или Admin)
- [ ] AUTH-3 (404 если курс/группа не найдены)
- [ ] VAL-1, VAL-2

**API:**
- `POST /api/courses/{courseId}/groups` — назначить группы
- `GET /api/courses/{courseId}/groups` — список групп
- `DELETE /api/courses/{courseId}/groups/{groupId}` — отвязать

**UI:**
- Раздел «Группы» на странице редактирования курса с мультиселектом групп
- UI-1, UI-3, UI-4

**Зависимости:** CourseService, GroupService

---

### UC-48: Студент может просмотреть прогресс по курсу

**Критерии приемки:**
- [ ] `GET /api/my/courses/{id}/progress` — прогресс студента по курсу
- [ ] Прогресс содержит: процент выполнения курса, количество сданных/всего заданий, пройденных/всего тестов, средний балл за задания
- [ ] AUTH-1
- [ ] AUTH-3 (404 если курс не найден или студент не в группе)

**API:**
- `GET /api/my/courses/{id}/progress` — прогресс по курсу

**UI:**
- Индикатор прогресса на странице деталей курса (`/my/courses/[id]`)
- UI-1, UI-3

**Зависимости:** CourseService, AssignmentService, SubmissionService, TestingService

---

### UC-49: Заведующий отделением может управлять экзаменами и зачётами

**Критерии приемки:**
- [ ] `GET /api/exams` — список экзаменов/зачётов с фильтрами (группа, предмет, семестр)
- [ ] `POST /api/exams` — создать экзамен/зачёт (предмет, группа, дата, тип, преподаватель, семестр)
- [ ] `PUT /api/exams/{id}` — редактировать
- [ ] `DELETE /api/exams/{id}` — удалить
- [ ] Экзамен: предмет, группа, дата проведения, тип (Exam/Test), преподаватель, семестр, статус (Scheduled/Passed/Cancelled)
- [ ] PAG-1, PAG-2, PAG-3
- [ ] AUTH-2 (только Admin / Заведующий отделением)
- [ ] AUTH-3 (404 если экзамен не найден)
- [ ] VAL-1, VAL-2, VAL-3 (дата экзамена обязательна)

**API:**
- `GET /api/exams?groupId={id}&semesterId={id}&type={exam|test}&page={n}&pageSize={m}` — список
- `POST /api/exams` — создать
- `PUT /api/exams/{id}` — обновить
- `DELETE /api/exams/{id}` — удалить

**UI:**
- Страница `/admin/exams` с таблицей экзаменов и формой создания/редактирования
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** GroupService, TeacherService, SemesterService

---

### UC-50: Преподаватель может назначить пересдачу экзамена

**Критерии приемки:**
- [ ] `POST /api/exams/{examId}/retakes` — назначить пересдачу студенту (студент, дата, причина)
- [ ] `GET /api/exams/{examId}/retakes` — список пересдач по экзамену
- [ ] `PATCH /api/exams/{examId}/retakes/{id}` — изменить статус пересдачи (Scheduled/Passed/Failed)
- [ ] `DELETE /api/exams/{examId}/retakes/{id}` — отменить пересдачу
- [ ] AUTH-2 (только преподаватель или Admin)
- [ ] AUTH-3 (404 если экзамен/пересдача не найдены)
- [ ] VAL-1, VAL-2 (дата пересдачи обязательна)

**API:**
- `POST /api/exams/{examId}/retakes` — назначить
- `GET /api/exams/{examId}/retakes` — список
- `PATCH /api/exams/{examId}/retakes/{id}` — обновить статус
- `DELETE /api/exams/{examId}/retakes/{id}` — отменить

**UI:**
- Раздел «Пересдачи» на странице экзамена с таблицей студентов
- UI-1, UI-3, UI-4

**Зависимости:** ExamService, StudentService

---

### UC-51: Заведующий отделением может переводить студентов между группами

**Критерии приемки:**
- [ ] `PATCH /api/students/{id}/transfer` — перевести студента в другую группу
- [ ] Параметры: новая группа, причина перевода
- [ ] После перевода студент получает доступ к курсам новой группы
- [ ] После перевода студент теряет доступ к курсам старой группы
- [ ] История переводов хранится в профиле студента
- [ ] AUTH-2 (только Admin / Заведующий отделением)
- [ ] AUTH-3 (404 если студент или группа не найдены)
- [ ] VAL-1, VAL-2

**API:**
- `PATCH /api/students/{id}/transfer` — перевести студента
- `GET /api/students/{id}/transfers` — история переводов

**UI:**
- Кнопка «Перевести» на странице студента с выбором группы и причиной
- UI-1, UI-3, UI-4

**Зависимости:** GroupService, CourseService

---

### UC-52: Заведующий отделением может формировать список на стипендию

**Критерии приемки:**
- [ ] `POST /api/stipend/generate?semesterId={id}` — сформировать стипендиальный список на основе успеваемости за семестр
- [ ] `GET /api/stipend/lists` — список сформированных ведомостей
- [ ] `GET /api/stipend/lists/{id}` — детали ведомости (студенты, суммы)
- [ ] `DELETE /api/stipend/lists/{id}` — удалить ведомость
- [ ] Алгоритм: студенты без задолженностей, средний балл ≥ 4.0, не имеющие дисциплинарных взысканий
- [ ] AUTH-2 (только Admin / Заведующий отделением)
- [ ] VAL-1, VAL-2

**API:**
- `POST /api/stipend/generate?semesterId={id}` — сформировать
- `GET /api/stipend/lists` — список ведомостей
- `GET /api/stipend/lists/{id}` — детали
- `DELETE /api/stipend/lists/{id}` — удалить

**UI:**
- Страница `/admin/stipend` с кнопкой генерации и списком ведомостей
- UI-1, UI-2, UI-3, UI-4

**Зависимости:** SemesterService, StudentService, SubmissionService

---

## Сводная таблица

| Сервис | User Stories | Статус реализации | Приоритет |
|--------|-------------|-------------------|-----------|
| SiteService | UC-1 — UC-8 (8) | ✅ Все реализованы | — |
| AuthService | UC-9 — UC-15 (7) | ✅ Все реализованы | P1 |
| ScheduleService | UC-16 — UC-23 (8) | ✅ Все реализованы | P2 |
| LearningService | UC-24 — UC-38, UC-46 — UC-52 (22) | ✅ Все реализованы | P1 |
| TestingService | UC-39 — UC-45 (7) | ✅ Все реализованы | P0 |

**Итого: 52 User Stories** — все реализованы.
