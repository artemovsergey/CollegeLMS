# Design: Обновление публичного сайта, админки, новостей и курсов (2026-07-31)

**Автор:** Architect
**Дата:** 2026-07-31
**Статус:** утверждён пользователем 2026-07-31
**Ветка:** `feature/site-admin-news-courses-overhaul`

---

## 1. Контекст

Пользователь дал 26 задач по CollegeLMS. После уточняющих вопросов приняты решения:

| Вопрос | Решение |
|---|---|
| Логотип → ссылка | Ведёт на `/` (корень текущего сайта); проверить все места (login desktop-панель — добавить) |
| Каскадное удаление пользователя | Переприсваивание: новости → системный админ; курсы → админ (с авто-созданием Teacher-профиля); личные профили Teacher/Student удаляются; сдачи студента удаляются каскадом |
| `/lms` | Редирект по роли: Student → `/my/dashboard`, Teacher → `/teacher/dashboard`, Admin → `/admin`, Dispatcher → `/dispatcher/dashboard`, неавторизован → `/login` |
| Семестры | Удалить полностью: фронт + бекенд + миграция |
| mdk0901 | Полный перенос контента, Angular → React в разделах 5 и 7 |
| Достижения | Контент спарсить со старого сайта stvcc.ru |
| Кнопка «Войти» | В главное меню хедера (ряд 2) |
| Задания | Тип занятия «Задание» — показываются в общем списке «Занятия» вместе с лекциями/практиками/СР (модель Assignment остаётся) |

---

## 2. Блок 1: Публичный сайт

### 2.1 Логотип — ссылка на корень
- Header.tsx:93 — уже `Link href="/"` ✓
- `/login` desktop-панель (app/login/page.tsx:51-58) — обернуть Image в `Link href="/"`
- Carousel — проверить логотип кликабельным

### 2.2 Выпадающие меню в хедере (hover + анимация)
- Данные уже есть: `data/site-content.ts` → `siteNavigation[].subsections`
- Desktop (`components/Header.tsx`, nav в ряд 2):
  - Для разделов с subsections — кнопка-триггер со стрелкой (ChevronDown), панель dropdown (absolute, top-full)
  - Анимация: `opacity-0 → 100` + `translate-y-1 → 0`, transition 150-200ms ease-out; при уходе курсора — fade с задержкой ~120ms
  - Доступность: `focus-within` открывает меню, Escape закрывает, `role="menu"`/`aria-expanded`
- Подпункты ведут на `section.href + subsection.slug` (catch-all `SectionPage` уже работает)
- Mobile (бургер): аккордеон — клик по разделу раскрывает подпункты (ChevronDown rotate)
- Пункт «Войти»: последний элемент главного меню, иконка User (lucide), `href="/login"`, в мобильном меню тоже
- Компонент `components/MegaMenu.tsx` — мёртвый, не трогаем (или удаляем при рефакторинге)

### 2.3 Канал Max
- Header.tsx:13: `https://max.ru/stvcc` → `https://max.ru/id2634028465_gos`
- Проверить упоминания max.ru в Footer и других местах

### 2.4 Единый стиль контента (самый объёмный пункт)
- Все подстраницы 5 разделов (Колледж, Образование, Поступление, Студенту, Сотруднику) из `data/page-contents.json` рендерятся через `SectionPage` + `ContentRenderer` с `.docs-content`
- Задача: привести весь контент к единому виду, как сделано со специальностями:
  - Стилизовать `.docs-content` (globals.css:270-370): заголовки h2/h3, списки, таблицы, цитаты, ссылки, картинки, аккордеоны FAQ
  - Типовые компоненты для структурирования: Hero-блок, InfoCard, List с иконками, Table, Accordion, Callout
  - Пройти каждый раздел по чек-листу: заголовки единого стиля, списки маркированные, таблицы оформлены, изображения с caption, ссылки-кнопки
- **Аудит ссылок**: скрипт (Node/PowerShell) извлекает все `href` из `page-contents.json` → проверить HTTP-статусы → битые проверить на stvcc.ru вручную → нерабочие: убрать ссылку/контент. Результат — отчёт + правки контента

### 2.5 Поиск — убрать дублирующий блок
- `app/(public)/search/page.tsx:271` — пустое состояние «Введите поисковый запрос...»
- Заменить на блок «Последние новости» (3-5 карточек через `GET /api/news`) при `!query`

### 2.6 Постер новости (детальная страница)
- `app/(public)/news/[id]/page.tsx` — верхний блок: `grid grid-cols-1 lg:grid-cols-3`
  - Левая колонка (1/3): синий фон `bg-primary`, дата, категория, заголовок, анонс (2-3 строки)
  - Правая колонка (2/3): картинка `object-cover`, клик → лайтбокс
- Текст гарантированно не пересекается с картинкой

### 2.7 Пункт меню «Достижения»
- `siteNavigation` + `page-contents.json`: новый раздел «Достижения»:
  - «Профессионалы» — чемпионатное движение (slug `professionals`)
  - «Мастер года» (slug `master-year`)
- Контент: спарсить со stvcc.ru (HTML), вставить в `page-contents.json`, оформить в едином стиле

### 2.8 Кнопка «Войти»
- В ряд 2 главного меню (после пунктов меню), иконка User + текст «Войти», `href="/login"`
- В мобильном бургер-меню — пункт «Войти» в конце

### 2.9 `/lms` — редирект по роли
- `app/lms/page.tsx` → серверный редирект:
  - без токена → `/login`
  - Student → `/my/dashboard`
  - Teacher → `/teacher/dashboard`
  - Dispatcher → `/dispatcher/dashboard`
  - Admin → `/admin`
- Использовать `useAuth` + `useEffect`/`redirect`

### 2.10 Контакты — выравнивание
- `app/(public)/contacts/page.tsx`: карта `h-full` + `min-h-[400px]`, контейнер контактов и карта выровнены по высоте в grid

### 2.11 Футер — убрать дубли
- `components/Footer.tsx`:
  - Удалить мёртвый `infoLinks`
  - Имя колледжа выводить один раз (бренд-блок), убрать дубли ссылок «Специальности»/«Контакты»
  - Битые ссылки (`/professions`, `/additional-education`, `/documents/*`, `/sveden`, `/privacy`): либо ведут на существующие страницы, либо удаляются
  - Реквизиты: оставить как есть (ИНН/КПП/ОГРН)

### 2.12 `/login` — быстрый вход выше формы
- Блок «Быстрый вход (разработка)» (app/login/page.tsx:133-151) переместить ВЫШЕ формы (под заголовок «Личный кабинет»)

---

## 3. Блок 2: Пользователи и роли

### 3.1 Удаление `IsActive` и жёсткое удаление пользователя
- Backend:
  - Удалить `User.IsActive` + миграция `DropColumn`
  - `AuthService` — убрать проверку «Пользователь деактивирован»
  - `DELETE /api/users/{id}` — жёсткое удаление (`db.Users.Remove`), с переприсваиванием:
    - `News.CreatedById` → Id системного админа (первый Admin)
    - `Course.TeacherId` → Teacher-профиль системного админа (авто-создать `Teacher { UserId = adminId, ... }`, если нет)
    - `Teacher.UserId`/`Student.UserId` — профили удаляются вместе с пользователем
    - `AssignmentSubmission.StudentId` — `Cascade` (сдачи удаляются со студентом)
    - `ScheduleEntry` (расписание преподавателя, если есть FK) — удалить/переприсвоить
  - Проверить все FK на User в конфигурациях и обновить поведение
- DTO: убрать `IsActive` из `UserResponse`/`CreateUserRequest` (если есть)
- Frontend: убрать тумблер/кнопку деактивации (красный текст на синем фоне исчезает), кнопка «Удалить» с AlertDialog-подтверждением «Удалить пользователя? Все связанные данные будут переприсвоены или удалены»
- Тесты: обновить UserServiceTests, UserControllerTests

### 3.2 Профиль пользователя по клику
- Строка таблицы `/admin` → клик → `/admin/users/[id]`
- Новая страница: карточка данных (логин, email, ФИО, роль), его курсы (если преподаватель), его новости (если автор)
- API: `GET /api/users/{id}` — дополнить связанными данными (или отдельный эндпоинт `/api/users/{id}/profile`)

### 3.3 Валидация форм (общий механизм)
- Backend: FluentValidation уже отдаёт `ValidationProblemDetails` с `errors` по полям. Исправить `UserService`:
  - `CreateAsync` — проверка уникальности Email + Login
  - `UpdateAsync` — Email обновляется (сейчас баг: игнорируется)
- Frontend: единый компонент `components/FormField.tsx` (label + input + error + hint-tooltip) и `FormErrorBanner.tsx`:
  - Интерсептор/helper `parseErrors(err)`: 400 с `errors` → вернуть `{ fieldErrors: { field: [msg] } }`; 409/бизнес → `{ message }`
  - Подсветка полей: `border-destructive` + текст ошибки под полем (не tooltip-скрытый — видимый текст, доступный)
  - Применить ко всем формам: пользователи, новости, курсы, группы, преподаватели, студенты, логин, профиль
- Валидаторы: сообщения на русском (уже), добавить подсказки-описания полей (hint)

### 3.4 Dashboard по ролям
- Student → `/my/dashboard` (есть), Teacher → `/teacher/dashboard` (есть), Admin → `/admin` (есть)
- Dispatcher → новая страница `/dispatcher/dashboard`: сводка расписания на неделю + группы
- Меню drawer (`app/(authenticated)/layout.tsx`): Dispatcher-пункты уже есть — добавить Dashboard в начало

### 3.5 Единое меню админа
- `AuthenticatedShell` (`components/AuthenticatedShell.tsx`): для роли Admin показывать полное админ-меню (секции «Система», «Обучение», «Финансы», «Расписание») во всех разделах — и в `/courses`, и в `/admin`
- `app/(authenticated)/layout.tsx` — roleMap: Admin получает все пункты
- Исправить баг «смена drawer при заходе в Курсы»

---

## 4. Блок 3: Новости, импорт, ошибки

### 4.1 Удаление статуса новости `IsPublished`
- Backend: удалить поле из `News`, DTO, маппера, валидатора; миграция `DropColumn`
- `NewsService`: создание → сразу опубликовано (`PublishedAt = DateTime.UtcNow`); обновление — без переключения статуса
- Frontend: убрать чекбокс «Опубликовано» из модалок, тумблер из таблицы
- Тесты: обновить NewsServiceTests/NewsControllerTests

### 4.2 Импорт — подсказка
- Рядом с кнопкой «Импорт» (`/admin/news`, `/admin/import`): блок-подсказка: «Импорт новостей и категорий из WordPress (stvcc.ru). Импорт асинхронный — можно покинуть страницу и вернуться позже»

### 4.3 Toast для 500 и 504
- `lib/api.ts` интерсептор (после 401-обработки):
  - `status === 500` → `toast.error("Ошибка сервера. Попробуйте позже")`
  - `status === 504` → `toast.error("Сервер недоступен. Проверьте соединение")`
  - Не показывать тост, если 500 получен в polling импорта? Нет — показывать всегда, но без дублей (debounce)
- Toaster уже подключён (sonner) в `app/layout.tsx`

### 4.4 Импорт — асинхронность через ImportJob
- Таблица `ImportJob` уже существует (миграция `AddImportJob`)
- `WordPressImportService`: сохранять прогресс (status, processed, total, error) в `ImportJob` в БД (вместо/в дополнение к in-memory ConcurrentDictionary)
- Эндпоинты: `POST /api/import/wordpress/rest` — создаёт ImportJob; `GET /api/import/wordpress/status/{id}` — читает из БД; `GET /api/import/wordpress/active` — возвращает последний незавершённый
- Frontend: убрать зависимость от localStorage, при загрузке страницы `/admin/import` — восстановление из `active` (уже реализовано частично)
- Импорт переживает переход по меню и рестарт сервиса

### 4.5 Модалка новости: скролл + HTML
- `components/ui/dialog.tsx`: `DialogContent` → `max-h-[85vh] overflow-y-auto` (глобально для всех модалок)
- `app/admin/news/page.tsx` модалка редактирования:
  - Textarea с HTML, `rows={8}`, monospace, скролл
  - Кнопка «Предпросмотр» — рендер через `ContentRenderer` в отдельном блоке модалки (или второй вкладке)
- Если HTML слишком много — textarea растягивается, модалка скроллится

### 4.6 Галерея новости — отступы
- `app/(public)/news/[id]/page.tsx` — лайтбокс/плитки: `gap-4` (сейчас ~2), отступы между плитками увеличить; в лайтбоксе добавить `p-4`

### 4.7 Превью картинки при создании новости
- `app/admin/news/page.tsx` (строки ~332-343): `object-cover` + фикс. высота 128px → показать всю картинку:
  - `object-contain` + контейнер с максимальной высотой (например `max-h-64`) и `h-auto`, или `Image` с `width/height` из файла
  - Пропорционально, без обрезки

### 4.8 Валидация форм (единый механизм из 3.3)
- Применяется ко всем формам: news, courses, groups, teachers, students, feedback, login
- Backend-валидаторы уже есть (FluentValidation) — проверить русские сообщения и покрытие полей

---

## 5. Блок 4: Обратная связь + таблицы

### 5.1 Пагинация feedback
- Backend: `GET /api/feedback` → `PagedResponse<FeedbackListItemDto>` (page, pageSize)
- Frontend: `/admin/feedback` — серверная пагинация (Pagination-компонент), убрать клиентский `slice`
- Убрать раскрытие строки по клику (line-clamp toggle) — только кнопка «Просмотреть» (модалка)
- Тесты: обновить FeedbackServiceTests/FeedbackControllerTests

### 5.2 Единый вид таблиц
- Вынести общий компонент `components/DataTable.tsx` (обёртка `rounded-lg border bg-card` + компактная таблица `text-sm p-2` + пустое состояние + загрузка)
- Все админ-таблицы (users, news, feedback, courses, groups, teachers, students, semesters(удаляется), specialties, exams, stipends, testing) → единый стиль
- `whitespace-nowrap` только там, где нужно (ячейки с длинным текстом — `truncate max-w`)

---

## 6. Блок 5: Курсы

### 6.1 Полный перенос mdk0901
- Репозиторий: `https://github.com/artemovsergey/mdk0901` (папки `course/`, `resources/`, `tutorial/`)
- Создать курс «МДК 09.01 Проектирование и разработка веб-приложений» (специальность ИП):
  - 9 разделов → структура занятий с нумерацией
  - 39 лекций, 46 практик, 10 самостоятельных работ
  - Контент из репозитория (HTML в `course/`) → Markdown (для Lecture.Content)
  - **Angular → React**: разделы 5 (Angular) и 7 (интеграция клиент-сервер) переписать под React (лекции + практики)
  - Преподаватель по умолчанию: ivanova (seed) — Teacher курса
  - Студент по умолчанию: student01 — записан в группу курса (CourseGroup)
- Способ загрузки: контент курса подготовить как JSON-ресурс в репозитории (`import/mdk0901_course.json`) + идемпотентный seed-метод в `DataSeeder` (как WordPress-импорт), который создаёт курс, занятия и привязки при старте
- Старые курсы с пустыми лекциями — не трогаем

### 6.2 Типы занятий
- Backend: `LectureType` enum: `Lecture` (Лекция), `Practice` (Практическая работа), `SelfStudy` (Самостоятельная работа)
- `Lecture.LectureType` + `HasConversion<string>` + `HasMaxLength` + миграция (default = Lecture)
- DTO/маппер: добавить `lectureType`
- Frontend: цветные Badge — Лекция (синий), Практика (зелёный), СР (янтарный), Задание (фиолетовый)
  - Цвета задать в едином map `LECTURE_TYPE_STYLES`
- Формы создания/редактирования занятия: Select «Тип занятия»
- Общий список занятий (для курса, для студента) — с цветами

### 6.3 Вкладки курса: Занятия / Материалы / Группы
- `app/(authenticated)/courses/[id]/page.tsx`:
  - Вкладка «Занятия»: единый список Lecture + Assignment (тип «Задание», фиолетовый бейдж), отсортированный, с цветными бейджами типов
  - Вкладка «Материалы»: общие файлы курса (CourseMaterial без привязки к занятию — уже так по модели)
  - Вкладка «Группы»: добавление (есть) + удаление (фикс)
  - Вкладки «Лекции»/«Задания» (старые) → объединяются в «Занятия»; «Материалы» остаётся
- Студенческий вид (`my/courses/[id]`) — те же вкладки

### 6.4 Фикс удаления группы с курса
- Отладить `DELETE /api/courses/{courseId}/groups/{groupId}` (бекенд) и кнопку на фронте
- Диагностировать: вероятно фронт не вызывает эндпоинт или ошибка 404/405, либо бекенд не находит запись
- Тест: удаление группы с курса (integration)

### 6.5 Markdown-редактор занятия
- `app/(authenticated)/courses/[id]/lectures/[lectureId]/page.tsx` — режим редактирования:
  - Textarea markdown + кнопка «Предпросмотр» (рендер markdown → HTML)
  - Зависимость: добавить `react-markdown` в CollegeLMS.Next
- Кнопка «Удалить занятие» (DELETE есть на беке) + AlertDialog подтверждение
- Новое занятие — там же выбор типа

### 6.6 Удаление раздела «Семестры»
- Frontend: удалить `/admin/semesters` страницу, пункт меню, ссылки
- Backend: удалить `SemesterController`, `SemesterService`, `ISemesterService`, `Semester` entity, DTO, маппер; миграция `DropTable("semesters")`
- Учесть FK: `Semester` не должен быть связан с другими сущностями (проверить)
- Тесты: удалить Semester-тесты

---

## 7. Блок 6: AGENTS.md + общие проверки

### 7.1 AGENTS.md — адаптивность
- Добавить в раздел «Соглашения по фронтенду»:
  - «Адаптивность проверяется на: ноутбук Toshiba A665 12k, телефон Xiaomi Mi 9 SE (2340×1080), широкие экраны (1920+). Все страницы должны корректно смотреться на всех перечисленных устройствах.»

### 7.2 Проверки после каждого блока
- `dotnet build` + `dotnet test` (G1)
- `npm run build` / `npm run dev` (G2)
- E2E Playwright: критичные flow (логин, редиректы по ролям, курсы, новости)
- `docker compose up --build -d` (G3) — в конце

---

## 8. Порядок реализации (блоки независимы, но есть зависимости)

1. **Блок 2 → Блок 3** (общий механизм валидации форм нужен новостям)
2. **Блок 1** (публичный сайт) — независим
3. **Блок 4** — независим, использует DataTable из 5.2
4. **Блок 5** — самый большой, независим
5. **Блок 6** — в конце

## 9. Открытые вопросы (решаются при реализации)

- Формат контента лекций: Markdown (не HTML) — бекенд принимает строку, фронт рендерит
- Способ загрузки курса mdk0901: скрипт-сидер (C#) или миграция с HasData — выбран скрипт/seed при старте (как WordPress-импорт), идемпотентно
- react-markdown — добавляется зависимость в CollegeLMS.Next
