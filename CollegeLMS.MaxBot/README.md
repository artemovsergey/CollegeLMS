# Бот расписания ГБПОУ СКС — Мессенджер MAX

## Что умеет бот

- **Только настройка уведомлений и их доставка**: бот присылает ежедневный
  дайджест расписания и уведомления об изменениях в чат MAX.
- Простая настройка: роль, группа (для студентов) или преподаватель,
  дни и время уведомлений — всё кнопками в боте.
- Расписание, избранное и изменения смотрят в **мини-приложении** MAX
  (кнопка приложения в профиле бота, ссылка `https://stvcc.tech/max`).
- Текущий выбор (группа или преподаватель) хранится в боте и **синхронизируется
  с мини-приложением**: можно выбрать и в боте, и в приложении — уведомления
  начнут приходить по этому выбору.

---

## Быстрый старт

### 1. Найди бота

Открой мессенджер **MAX** и найди бота по имени или нику (указывается при создании бота на платформе).

### 2. Нажми «Начать»

Нажми кнопку **«Начать»** или отправь команду `/start`.

При первом запуске бот пришлёт приветствие — название и описание, что он
умеет, — и покажет главное меню. Если выбор уже сохранён, приветствие не
повторяется: бот сразу открывает меню с текущим выбором.

### 3. Главное меню

| Кнопка | Что делает |
|--------|------------|
| **🎓 Студент** | Найти и выбрать свою группу |
| **👨‍🏫 Преподаватель** | Найти и выбрать себя в списке преподавателей |
| **⚙️ Настройки** | Уведомления и смена текущего выбора |

Пока выбор не задан, меню пишет: «Текущий выбор: не задан».

### 4. Найди себя поиском

- **Студент:** бот попросит написать номер или название группы — отправь
  сообщением, например `ИС-21`. Бот покажет найденные группы кнопками;
  если результатов много — листай страницы кнопками `← Назад` / `Далее →`.
  Нажми на свою группу.
- **Преподаватель:** аналогично — напиши ФИО или его часть и выбери себя
  из списка.

Если поиск не нужен — кнопка **«🔙 Отмена»** вернёт в главное меню.
После выбора бот покажет главное меню с текущим выбором и кнопками
**📱 Открыть расписание** и **⚙️ Настройки**.

---

## Меню

Управление — только кнопками, команды писать не нужно.

Главное меню показывает текущий выбор:

| Состояние | Что видно |
|-----------|-----------|
| **выбор не задан** | «Текущий выбор: не задан» и кнопки **🎓 Студент**, **👨‍🏫 Преподаватель**, **⚙️ Настройки** |
| **группа выбрана** | «Группа: ИС-21» и кнопки **📱 Открыть расписание**, **⚙️ Настройки** |
| **преподаватель выбран** | «Преподаватель: Иванов И. И.» и кнопки **📱 Открыть расписание**, **⚙️ Настройки** |

Уведомления настраиваются внутри **⚙️ Настройки**.

Служебные команды (для совместимости): `/start`, `/settings`, `/help`.

---

## Мини-приложение

Расписание, избранное и изменения доступны в **мини-приложении** MAX —
отдельной странице бота (открывается кнопкой приложения в MAX или ссылкой
`https://stvcc.tech/max`):

| Раздел | Что внутри |
|--------|------------|
| **Расписание** | День / неделя, выбор даты, поиск группы и преподавателя |
| **Избранное** | Быстрый доступ к своим группам и преподавателям (хранится на устройстве) |
| **Изменения** | Лента корректировок расписания (карточки как на сайте) |
| **Журнал** | Доступен преподавателям |

### Текущий выбор и синхронизация с ботом

Текущий выбор общий для бота и мини-приложения:

- если выбор не задан, приложение предложит найти группу или преподавателя
  и сделает выбор текущим;
- кнопка **«Выбрать»** в поиске (и «Открыть» в избранном) сохраняет выбор
  в боте — бот тут же присылает в чат текущий выбор с кнопками
  **📱 Открыть расписание** и **⚙️ Настройки**, а уведомления начинают
  приходить по этому выбору;
- выбранная группа или преподаватель помечаются в поиске бейджем
  **«Текущий»**.

Кнопка **«📱 Открыть расписание»** в главном меню и кнопки-открытия в
уведомлениях открывают мини-приложение сразу на нужном дне или разделе:
бот передаёт в приложение маршрут, дату и группу/преподавателя.
Если выбор сделан в боте, мини-приложение откроется по нему же.

Диспетчер работает **только в веб-приложении** (роль Dispatcher) — в боте и
мини-приложении его нет.

---

## Настройки

Из главного меню нажми **⚙️ Настройки** — бот покажет текущий выбор
и предложит:

| Кнопка | Что делает |
|--------|------------|
| **🔔 Уведомления** | Включить/выключить уведомления, выбрать дни недели и время |
| **🎓 Студент** | Найти и выбрать группу (роль меняется на «студент») |
| **👨‍🏫 Преподаватель** | Найти и выбрать себя в списке (роль меняется на «преподаватель») |
| **🔙 Меню** | Вернуться в главное меню |

---

## Уведомления

**⚙️ Настройки** → **🔔 Уведомления**:

- **Статус** — «Расписание на день: включено ✅» или «выключено ❌»
  (кнопка-переключатель);
- **Дни** — нажми на день недели, чтобы включить или выключить его
  (по умолчанию — по будням, Пн–Пт);
- **Время до начала занятий** — кнопки `⏰ −5 мин` / `⏰ +5 мин`, диапазон
  07:30–08:30 (МСК). По умолчанию расписание на день приходит в **07:30**.

Изменения сохраняются сразу, отдельной кнопки «Сохранить» нет.

### Ежедневный дайджест

Каждое утро в выбранное время бот присылает расписание на сегодня — если этот
день включён в настройках.

Пример сообщения:
```
📋 Понедельник, 07.09 — Группа

1. 📖 Математика
    🕐 09:10–10:40  📍 301
    👨‍🏫 Иванов И.И.

2. ✏️ Физика
    🕐 10:50–12:20  📍 205
    👨‍🏫 Петров П.П.
```

Если пар нет — бот напишет: «Расписания нет — выходной!».

### Уведомления об изменениях

Когда диспетчер применяет корректировку, подписчики получают сообщение с
карточками изменений — так же лаконично, как в веб-версии:

```
🔔 Изменения в расписании
📅 Понедельник, 21.09.2026

▫️ ИТ-21 · Вторник · нед. 3 · пара 2
    📖 «Математика» → «Физика» — замена
    📍 ауд. 204
    👨‍🏫 Петров П.П.

📱 Открыть: https://stvcc.tech/max?route=day&date=2026-09-22
```

---

## Пример диалога

```
Пользователь: /start

Бот: 📚 Расписание колледжа
     Я бот расписания: слежу за изменениями и присылаю расписание на день.
     Что умею:
     • 📅 открывать расписание на день и неделю в мини-приложении;
     • 🔔 присылать расписание на день и уведомления об изменениях;
     • ⭐ хранить избранные группы и преподавателей.
     Выбери, кто ты, и найди себя поиском.
     🏠 Главное меню
     Текущий выбор: не задан
     [🎓 Студент] [👨‍🏫 Преподаватель] [⚙️ Настройки]

Пользователь: [🎓 Студент]

Бот: 👨‍🎓 Студент
     Напиши номер или название группы — я найду её в списке.
     [🔙 Отмена]

Пользователь: ИС-21

Бот: 🔎 Группы по запросу <ИС-21> (стр. 1/1):
     [ИС-21] [ИС-21-2]

Пользователь: [ИС-21]

Бот: 🏠 Главное меню
     Группа: ИС-21
     [📱 Открыть расписание] [⚙️ Настройки]

Пользователь: [⚙️ Настройки]

Бот: ⚙️ Настройки
     Текущий выбор: ИС-21
     [🔔 Уведомления] [🎓 Студент] [👨‍🏫 Преподаватель] [🔙 Меню]

Пользователь: [🔔 Уведомления]

Бот: 🔔 Уведомления
     Расписание на день: включено ✅
     Дни: Пн, Вт, Ср, Чт, Пт
     ⏰ Время до начала занятий: 07:30 (МСК)
     [🔕 Выключить уведомления]
     [✓ Пн] [✓ Вт] [✓ Ср] [✓ Чт]
     [✓ Пт] [Сб] [Вс]
     [⏰ −5 мин] [⏰ +5 мин]
     [🔙 Назад]
```

---

## Частые вопросы

**Бот не находит мою группу?**
Пиши номер или название группы без лишних символов (например, `ИС-21`).
Убедись, что расписание загружено в систему (через админку или импорт XLSX).

**Уведомления не приходят?**
Проверь: **⚙️ Настройки** → **🔔 Уведомления** — включены ли уведомления,
нужный день отмечен, и наступило ли выбранное время. В выходные и нерабочие
дни (по календарю диспетчера) рассылки нет.

**Хочу сменить группу?**
**⚙️ Настройки** → **🎓 Студент** → найди новую группу поиском.
Или сделай выбор в мини-приложении — бот подхватит его.

**Где посмотреть само расписание?**
В мини-приложении бота (в MAX) — раздел **Расписание**. Бот присылает только
уведомления и настраивает их.

**Нужно внести изменения в расписание?**
Это делает диспетчер в веб-приложении (роль Dispatcher). После применения
корректировки подписчики получают уведомление.

---

## Разработка и развёртывание

### Запуск (Docker)

Бот поднимается в общем compose под профилем `max-bot`:

```powershell
docker compose --profile max-bot up --build -d
```

Конфигурация через env:

| Переменная | Назначение |
|-----------|------------|
| `MAX_BOT_TOKEN` | Токен бота (выдаётся на platform-api2.max.ru при создании бота) |
| `MAX_BOT_WEBHOOK_URL` | Публичный HTTPS-адрес вебхука (по умолчанию `https://stvcc.tech/maxbot/webhook`) |
| `MAX_BOT_WEBHOOK_SECRET` | Секрет вебхука (5–256 символов `[A-Za-z0-9_-]`); в деплое обязателен |
| `MAX_BOT_DB_NAME` | Имя БД (по умолчанию `collegelms_maxbot`) |
| `DB_USER` / `DB_PASSWORD` | Учётка PostgreSQL |

### БД

Настройки пользователей хранятся в **отдельной** базе `collegelms_maxbot`
(таблица `user_settings`). Схема создаётся автоматически при первом старте
(`EnsureCreated`), основная БД `collegelms` не затрагивается.

### Часовой пояс

По умолчанию `Europe/Moscow` (IANA). На Windows при необходимости можно
переключить на `Russian Standard Time` в `appsettings.json`
(`MaxBot:TimeZone`). Уведомления отправляются в выбранное время по МСК
(по умолчанию `07:30`).

### Обновления: webhook и long polling

В продакшене бот получает обновления через **webhook** (MAX не рекомендует
long polling для production): при старте он оформляет подписку
`POST /subscriptions` на `MaxBot:WebhookUrl` (по умолчанию
`https://stvcc.tech/maxbot/webhook`), а события принимает на
`POST /maxbot/webhook` (nginx проксирует `/maxbot/` в контейнер бота).

- `MaxBot:WebhookSecret` — секрет подписки; MAX присылает его в заголовке
  `X-Max-Bot-Api-Secret`, запросы с неверным секретом отклоняются (401).
- Обработка асинхронная: эндпоинт сразу отвечает `200`, апдейт попадает в
  очередь (`MaxUpdateQueue`) и обрабатывается так же, как при long polling.
- Если оформить подписку не удалось (3 попытки с интервалом 5 секунд), бот
  переходит на резервный long polling и одновременно продолжает читать
  очередь — доставленные события не теряются.

### Внутренние эндпоинты для мини-приложения

- `GET /maxbot/internal/users/{maxUserId}` — профиль пользователя MAX
  (роль, группа, преподаватель) для входа в мини-приложение.
- `POST /maxbot/internal/selection` — сохранить выбор группы или
  преподавателя, сделанный в мини-приложении; бот обновляет `user_settings`
  и отправляет в чат сообщение с текущим выбором.

Оба эндпоинта защищены заголовком `X-Internal-Secret` (`MaxBot:InternalSecret`);
если секрет не задан, они отвечают `401`.

### Кнопки мини-приложения

Кнопки «📱 Открыть расписание» и «📅 Открыть день» — типа `open_app` с полем
`web_app` (публичное имя бота, `MaxBot:BotPublicName`) и payload вида
`{route}[-yyyy-MM-dd][-g-<groupId>|-t-<teacherId>]`. Мини-приложение читает payload
из `start_param` MAX Bridge (`window.WebApp.initDataUnsafe.start_param`) и
навигирует на нужный экран.

### Учебная неделя

Номер недели для конкретной даты вычисляется от начала семестра `2026-09-01`
(см. `Services/StudyWeek.cs`) и передаётся в API как фильтр `week`; день —
фильтр `dayOfWeek`. Так формируется расписание на произвольную дату.

### Заметка про SSL

Клиент MAX игнорирует валидацию TLS-сертификата
(`DangerousAcceptAnyServerCertificateValidator`) — требуется для работы с
российским CA платформы в Docker. В продакшене допустимо только в связке с
локальным токеном и ограниченным доступом к контейнеру.

### Проверка

```
dotnet build CollegeLMS.slnx
dotnet test CollegeLMS.MaxBot.Tests
dotnet csharpier format CollegeLMS.MaxBot --check
```

*Бот расписания ГБПОУ СКС. Мессенджер MAX.*
