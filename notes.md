# Замечания

- надо придумать голосовой ввод
   
- лучше не просить запустить сервера, он запускает, но не останавливается

- проблема было, что Docker Desktop поставил себе настройки прокси. Просле очистки прокси все заработало

- модель Deepseek v4 не сразу правильно связала фронтенд и бекенд, но потом сразу быстро нашла проблему и решила ее

- на фазу настройки docker-compose тратится много времени, надо внести это в фазу 0 настройку

- в opencode в /variants использовать max

- Phase 6 (DevOps) нельзя считать выполненной без запущенного Docker Desktop — `docker compose build` падает с ошибкой соединения с демоном. Нужно запускать вручную перед мержем.
- надо замерить время выполнения каждой фазы, чтобы понять на что тратиться больше всего времени при разработке

- составить референсы сайтов, которые нравятся
- придумать специально домен для колледжа,как альтернатива stvcc.ru
- развернуть с доменом, ssl, на 